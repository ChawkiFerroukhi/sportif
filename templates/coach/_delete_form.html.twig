<form method="post" action="{{ path('app_coach_delete', {'id': coach.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coach.id) }}">
    <button class="btn"><i class="fa-solid fa-trash"></i></button>
</form>
