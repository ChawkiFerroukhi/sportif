<form method="post" action="{{ path('app_decaissement_delete', {'id': decaissement.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ decaissement.id) }}">
    <button class="btn"><i class="fa-solid fa-trash"></i></button>
</form>
