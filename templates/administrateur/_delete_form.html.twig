<form method="post" action="{{ path('app_administrateur_delete', {'id': administrateur.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administrateur.id) }}">
    <button class="btn"><i class="fa-solid fa-trash"></i></button>
</form>
